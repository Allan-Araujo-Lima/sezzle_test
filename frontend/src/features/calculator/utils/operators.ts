import type { Operator } from "../../../interfaces/calculator";

export const OPERATOR_SYMBOLS: Record<Operator, string> = {
    add: '+',
    subtract: '−',
    multiply: '×',
    divide: '÷',
    percent: '%',
    exponent: '^',
    square: '²',
};
